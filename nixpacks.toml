[phases.setup]
nixPkgs = ['nodejs_18', 'chromium', 'nssmdns']

[phases.install]
cmds = [
  'cd backend && npm install',
  'cd frontend && npm install'
]

[phases.build]
cmds = [
  'cd frontend && npm run build'
]

[start]
cmd = 'node backend/src/server.js'
